// import { useQuery } from '@tanstack/react-query';
// import itemService from '../services/item.service';

// export const useDetailed = (id: string) => {
//   return useQuery({
//     queryKey: ['id'],
//     queryFn: () => itemService.getById(id),
//     select: ({ data }) => data
//   });
// };
